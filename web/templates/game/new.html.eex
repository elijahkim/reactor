<h1>Create Game</h1>

<%= form_for @conn, game_path(@conn, :create), [as: :game], fn f -> %>
  <div>
    <%= label f, :name %>
    <%= text_input f, :name %>
  </div>

  <div>
    <%= label f, :up_to, "Play up to" %>
    <%= number_input f, :up_to %>
  </div>

  <%= submit "Create" %>
<% end %>
